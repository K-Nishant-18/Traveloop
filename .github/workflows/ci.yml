name: CI/CD Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # --- Backend Steps ---
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build and Test Backend (Maven)
        working-directory: ./backend
        run: mvn clean verify # 'verify' runs tests. Change to 'package -DskipTests' if tests fail.

      # --- Frontend Steps ---
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: ./frontend/package-lock.json

      - name: Install Frontend Dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Lint Frontend
        working-directory: ./frontend
        run: npm run lint

      - name: Build Frontend (Vite)
        working-directory: ./frontend
        run: npm run build

  docker-build-and-push:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' # Only run Docker build/push on merge, not on PRs
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Verify Docker Compose Build
        run: docker-compose build

      # --- CONTINUOUS DEPLOYMENT (CD) ---
      # Below is a template for pushing to GitHub Container Registry (GHCR)
      # Uncomment to enable CD!
      
      # - name: Log in to the Container registry
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      #
      # - name: Tag & Push Backend Image
      #   run: |
      #     docker tag traveloop_backend:latest ghcr.io/${{ github.repository }}/traveloop-backend:latest
      #     docker push ghcr.io/${{ github.repository }}/traveloop-backend:latest
      #
      # - name: Tag & Push Frontend Image
      #   run: |
      #     docker tag traveloop_frontend:latest ghcr.io/${{ github.repository }}/traveloop-frontend:latest
      #     docker push ghcr.io/${{ github.repository }}/traveloop-frontend:latest
